# Pickup notes — 2026-06-12

Where things stand after the MoneyField session on this machine. Branch: `chore/work-2026-06-09`. **Nothing is committed.**

## What got done

**MoneyField now supports a controlled `value` prop** (`value?: number | null`), mirroring PercentageField's prevValue/focused pattern — external value changes (resets, restored form state) are reflected, but never while the user is mid-edit. `defaultValue` behaviour is unchanged.

Files touched this session:

| File | Change |
|---|---|
| `src/components/MoneyField/index.tsx` | Controlled `value` prop + `formatValue` helper (matches blur normalisation, so no flicker when a parent echoes the committed value back) |
| `src/stories/components/MoneyField.stories.tsx` | New `Controlled` story (field + external reset button); behaviour notes updated |
| `docs/guidelines/components.md` | MoneyField entry: controlled/uncontrolled wording + `value` in key props (matches PercentageField's entry) |
| `.claude/launch.json` | Added a `storybook` config (port 6006) — works once the install is fixed (see below) |

Verified: typecheck + ESLint clean on changed files; live-tested the uncontrolled path in the dev app (retirement-projection flow — typing formats, blur normalises to `85,000.50`, value persists across step navigation). The controlled path follows PercentageField's proven implementation but **was not visually verified in Storybook** because Storybook won't start (below).

## To pick up

1. **Fix Storybook** — `npm run storybook` crashes: missing `@oxc-parser/binding-darwin-x64` native binding (npm optional-deps bug, [npm/cli#4828](https://github.com/npm/cli/issues/4828)). Try `npm install` first; full node_modules reinstall if that doesn't restore it. (Only an issue on this machine's install — your work machine may be fine.)
2. **Verify the `Controlled` story** in Storybook once it runs (Form Components / TextInput / MoneyField).
3. **Route the pipeline sign-offs** — this is a design-system API change, so per AGENTS.md it needs Moe (API), then Chalmers / Flanders / Marge / Lisa, then Willie's checklist. components.md and the story are already updated to keep Moe's and Lisa's artefacts accurate.
4. **Optionally migrate consumers** — `src/features/retirement-projection/steps/*.tsx` still use the defaultValue remount pattern; they can now pass `value` directly. Not done — out of scope for the component change.
5. **Commit selectively** — the working tree also has changes **not from this session**: `src/components/TextField/index.tsx`, `src/components/FormProgress/SteppedTrack.tsx`, `src/components/Header/UtilityBar.tsx`, `package-lock.json`, plus the untracked `src/features/retirement-projection/` + `src/app/retirement-projection/` work. Don't sweep those into the MoneyField commit.

## Environment quirks on this machine (skip if reading elsewhere)

- Shell defaults to Node 12 — `source ~/.nvm/nvm.sh && nvm use 20` before any npm/node command.
- Xcode CLT broken: use Homebrew git (`/usr/local/bin/git`), not `/usr/bin/git`.
