import Box from '@mui/material/Box';
import Typography from '@mui/material/Typography';

export default function AdviserOnlinePage() {
  return (
    <Box sx={{ p: { xs: 3, md: 4 } }}>
      <Typography variant="h1" sx={{ mb: 1 }}>Adviser Online</Typography>
      <Typography variant="body" color="text.muted">
        Adviser portal — page placeholder.
      </Typography>
    </Box>
  );
}
