- [x] FileUpload component the file upload progress bar needs to be replaced with the linearProgress component
- [x] FileUpload split out the file card into it's own component and all the different states
- [x] In storybook group these in a folder called fileUpload 

- [x] Icons, go through and check for new icons that have been added to the public / icons / font-awesome light and solid folders, check the icons in that folder and then check the icons that are listed in the Icon gallery in story book, any missing icons from the gallery, update it

- [x] LinearProgress component the end caps need to be rounded
- [x] remove the colours story, it's only primary

- [x] remove the StatMetric component

- [x] Table component remove the box shadow on the table container 
- [x] The table container I want to have rounded corners (16px)
- [x] Density default - row height I want to be 48px
- [x] Density condensed - row height to be 44px
- [x] Density spaced - row height to be 52px
- [x] In storybook I want you to illustrate the responsive behavior of overflow horizontal scroll
- [x] Setup the component so it can sort columns
- [x] remove the empty story
- [x] remove kitchen sink story
- [x] Sticky header isn't working, check this and fix it
- [x] Setup table pagination as per the data table example here [React Table component - Material UI](https://mui.com/material-ui/react-table/#data-table)
- [x] Should this table pagination be it's own component?
- [x] I want a responsive table component where it collapses into an accordion on mobile <sm, needs to be accessible and ideally one code set (e.g. I would like to avoid a table that's swaped out with an accordion on mobile)
- [x] Once setup I want you to group the table component and this responsive table component (and if you recommend table pagination being it's own component) into a folder in storybook called Tables
- [x] in responsive table component delete the mobile preview stories (they're redundant), delete pagination, empty, loading
- [x] The custom mobile summary column, the card header should always be on top collapsed or expanded (see expanded screen shot, designer should be on top)
- [x] Setup a stripped column variant in the stripped story 
- [x] Setup a bordered tables variant that has vertical and horizontal borders 

